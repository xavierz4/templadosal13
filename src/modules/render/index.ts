/**
 * @module render
 * @description Barrel file público del módulo render (Epic 5 — WebGL 3D Engine)
 * Regla 3 FSD: único punto de entrada externo para el módulo.
 */
export { default as Scene3DCanvas } from './ui/Scene3DCanvas.svelte';
